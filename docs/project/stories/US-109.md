## US-109: Playwright MCP Integration for Agent-Driven Browser Testing

**As** the QE agent,
**I want** Playwright available via MCP (Model Context Protocol) in the
development environment,
**so that** I can perform exploratory browser testing of the web UI during
review and acceptance workflows without manual intervention.

**Status:** DONE (Accepted 2026-03-29. AC #1-4 all done, DoD 3/3. QE verified end-to-end. Owner accepted — advisory sign-offs waived per owner directive for lightweight tooling story.)
**Priority:** High (QE exploratory testing blocked)
**Effort:** ~15 minutes (architect estimate)
**Depends on:** None (nixpkgs has `playwright-mcp` 0.0.56)
**Blocks:** QE exploratory testing of US-089 through US-098

### Background

The owner requested Playwright MCP integration this session. The architect
assessed it as straightforward: `playwright-mcp` 0.0.56 is available in
nixpkgs. QE's exploratory testing of the 8 stories in TEST phase (US-089
through US-098) is blocked without browser automation capability.

### Acceptance Criteria

- [x] AC #1: `playwright-mcp` added to `devShell` in `flake.nix`
- [x] AC #2: Wrapper script created that launches `playwright-mcp` with
      appropriate browser configuration (headless Chromium)
- [x] AC #3: `.claude/settings.json` created with MCP server configuration
      pointing to the wrapper script
- [x] AC #4: QE agent can use Playwright MCP to navigate to `http://localhost:8080`
      and interact with the web UI during a `local-demo` session
      *(verified 2026-03-29 — navigate, snapshot, screenshot, click, close all functional)*

### Definition of Done

1. `nix develop` shell includes `playwright-mcp` binary
2. MCP config in `.claude/settings.json` is valid and loads without errors
3. QE confirms browser automation works against local-demo web UI

### Notes

- This is distinct from US-037 (Playwright pytest scaffolding for automated
  test suites). US-109 is about MCP integration for agent-driven exploratory
  testing; US-037 is about CI test infrastructure.
- `playwright-mcp` provides browser automation as an MCP tool server,
  allowing Claude agents to drive a browser programmatically.

**Related:** US-037 (Playwright test scaffolding, different scope),
US-089-098 (stories needing QE browser testing)
