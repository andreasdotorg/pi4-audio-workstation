## US-148: Browser E2E for Status Bar -- Connection, Panic Button, Mode Dropdown

**As** the QE,
**I want** a browser-driven E2E smoke test for the status bar covering
connection status, panic button presence, and mode dropdown,
**so that** the primary operator controls are verified end-to-end through the
actual user interface.

**Status:** draft
**Priority:** P0
**Effort:** ~0.5 worker days
**Origin:** QE E2E audit (session 14)
**Depends on:** local-demo stack

### Acceptance Criteria

- [ ] Browser test opens the web UI
- [ ] Verifies connection status indicator shows "connected"
- [ ] Verifies panic button is present and clickable
- [ ] Verifies mode dropdown is present and shows current mode
- [ ] Test runs against the local-demo stack (not mocks)
- [ ] Test placed in `tests/e2e/` (genuine browser-driven E2E)
