## US-111: Local-Demo PipeWire Graph Topology Redesign

**As** a developer testing the web UI and audio pipeline locally,
**I want** the local-demo to use a room-simulator filter-chain that replaces
both the USBStreamer output and UMIK-1 capture with identical node names,
and loopback-based ada8200-in capture,
**so that** the local-demo faithfully exercises the production PipeWire graph
topology with realistic per-channel room acoustics and improved process cleanup.

**Status:** IMPLEMENT (session 4 — architect decomposed 13 tasks after scope revision, AE signed off)
**Scope revision (session 4):** T-111-01 spike confirmed WP required on PW 1.6.x. AC #4 (spa-node-factory clock) and AC #6 (WP elimination) DROPPED. WP retained with policy disabled. T-111-04 (clock driver config) dropped from task list. Owner to be informed at session end.
**Priority:** Medium (development workflow quality)
**Effort:** 1-2 worker days
**Origin:** Architect redesign proposal (session 3)
**Depends on:** US-075 (local PW test env — COMPLETE)
**Blocks:** US-067 (speaker-room-mic simulator — shares room IR approach)
**Decisions:** D-040 (PW filter-chain), D-043 (no WP auto-linking)

### Background

The current local-demo (`nix run .#local-demo`) uses WirePlumber with policy
disabled plus null-audio-sink adapter nodes for virtual hardware. This has
several problems:

1. WirePlumber is needed solely for adapter node port activation — it adds
   process complexity and is a source of zombie processes (F-216)
2. Null-audio-sink nodes don't behave identically to production hardware
   nodes — different port naming, different format negotiation
3. No acoustic simulation — the local-demo passes audio through Dirac
   (identity) impulses, so the spectrum display shows the raw signal rather
   than what would appear in a real room
4. UMIK-1 capture path untested — no simulated microphone input

The architect has produced a complete redesign that eliminates these issues.

### Design Summary (Architect)

**Core idea:** A single room-simulator filter-chain replaces both the
USBStreamer output sink AND the UMIK-1 capture source, using identical
PipeWire node names so GM's routing table works unmodified.

**Key components:**

1. **Room-sim filter-chain** — a PipeWire filter-chain module that:
   - Has a capture (sink) side with `node.name` matching USBStreamer output
     prefix (`alsa_output.usb-MiniDSP_USBStreamer...`)
   - Has a playback (source) side with `node.name` matching UMIK-1 capture
     prefix
   - Internally convolves each input channel with a per-channel room impulse
     response (4 IRs: left speaker, right speaker, sub1, sub2 — each with
     different propagation delays and room characteristics)
   - Sums the convolved outputs to produce a simulated mono microphone signal
   - The simulated mic signal appears on the source side, exercising the
     full measurement capture path

2. ~~**Clock driver via `spa-node-factory`**~~ — DROPPED (T-111-01 spike:
   `spa-node-factory` clock driver does not activate filter-chain ports on
   PW 1.6.x. WirePlumber required for port activation.)

3. **ada8200-in via loopback module** — a PipeWire loopback module creates
   the ada8200-in capture node. Loopback modules are self-activating.

4. ~~**WirePlumber eliminated**~~ — DROPPED. WP retained with policy
   disabled (`policy.standard = disabled`, `policy.node = required`).
   Process cleanup improved but WP remains in process tree.

### Audio Engineer Requirements (signed off)

The AE approved the design with 6 requirements:

1. Per-channel room IRs must model different speaker positions (different
   propagation delays, different early reflection patterns)
2. Sub channels must have appropriate LF room gain (~3-6 dB boost below
   80 Hz from room modes)
3. Propagation delays must be physically plausible (1-5ms range for typical
   venue distances)
4. The simulated microphone signal must be mono (single-channel sum of all
   speaker contributions)
5. Room IRs should be short enough for low CPU overhead (512-1024 taps at
   48 kHz = 10-21ms, sufficient for early reflections)
6. The simulation need not be physically accurate — it must be "good enough"
   to exercise the measurement pipeline and produce visually meaningful
   spectrum/level displays

### Acceptance Criteria

- [ ] AC #1: Room-sim filter-chain replaces USBStreamer output node — sink
      side has `node.name` matching GM's `USBSTREAMER_OUT_PREFIX`, 8 channels
      with `playback_AUX0` through `playback_AUX7`
- [ ] AC #2: Room-sim filter-chain provides UMIK-1 capture — source side has
      `node.name` matching GM's UMIK-1 prefix, mono output representing
      simulated microphone signal
- [ ] AC #3: 4 per-channel room IRs with different propagation delays
      (physically plausible, 1-5ms range) and room characteristics (LF boost
      on sub channels)
- [x] AC #4: ~~`support.node.driver` via `spa-node-factory` provides graph
      clock — no WirePlumber dependency~~ DROPPED (T-111-01 spike: not viable on PW 1.6.x)
- [ ] AC #5: ada8200-in via PipeWire loopback module — self-activating,
      8 channels, `node.name = "ada8200-in"` matching GM routing constant
- [x] AC #6: ~~WirePlumber completely removed from local-demo process tree~~ DROPPED (WP required for port activation on PW 1.6.x)
- [ ] AC #7: GM routing works unmodified — production `RoutingTable` matches
      all node names in the redesigned local-demo graph
- [ ] AC #8: Web UI spectrum display shows room-affected signal (not flat
      Dirac passthrough) — visually confirms simulation is active
- [ ] AC #9: Measurement pipeline exercisable end-to-end in local-demo:
      sweep -> capture via simulated UMIK-1 -> deconvolve -> correction
- [ ] AC #10: Process cleanup improved — reduced zombie accumulation
      (F-216 partially addressed). WP retained but cleanup handlers cover
      WP process. All child processes terminate when local-demo exits.
- [ ] AC #11: Room IR WAV files committed to `tests/fixtures/room-irs/`
      (generated once, checked in)

### Definition of Done

- [ ] All acceptance criteria met
- [ ] Architect review: filter-chain config, node naming, clock driver approach
- [ ] AE review: room IR plausibility, propagation delays, spectral characteristics
- [ ] QE review: GM routing verification, process cleanup, measurement pipeline E2E
- [ ] No regressions in existing test suite (`test-all`, `test-integration-browser`)
- [ ] `nix run .#local-demo` works with redesigned topology

### Technical Notes

- Room IRs can be generated with a simple Python script using scipy: exponential
  decay envelope, random early reflections, per-channel delay offset, LF shelf
  for sub channels
- ~~The `spa-node-factory` approach for clock driver~~ — T-111-01 spike
  confirmed this does not work on PW 1.6.x: filter-chain ports require WP
  for activation even with `support.node.driver` and `node.group`. This
  confirms the session 3 empirical finding.
- Filter-chain config format is the same as the production convolver config —
  just different coefficient files and node names
- This design is a stepping stone toward US-067 (full physics-based simulation)
  but much simpler — no T/S parameter modeling, no ray tracing, just
  representative IRs

### Related

US-075 (local PW test env — predecessor, COMPLETE), US-067 (speaker-room-mic
simulator — future, shares room IR approach), F-216 (zombie processes —
addressed by AC #10), F-203 (zombie cleanup — partial fix), D-040 (PW
filter-chain), D-043 (no WP auto-linking)
