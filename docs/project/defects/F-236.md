## F-236: UMIK-1 spectrum in test tab shows frequency-dependent rolloff (OPEN)

**Filed:** 2026-03-31 (session 6)
**Severity:** Medium
**Status:** Open
**Affects:** Web UI test tab spectrum display, UMIK-1 capture visualization
**Found by:** Owner (session 6, morning testing)

### Description

The UMIK-1 spectrum display in the test tab shows frequency-dependent
rolloff — higher frequencies are increasingly silent. This is similar to
a previously fixed bug in the main dashboard spectrum display.

### Likely Cause

Code duplication — the test tab spectrum rendering may share a bug that
was already fixed in the dashboard spectrum. If the FFT pipeline or
spectrum rendering was duplicated (see F-101 / F-099 history of duplicate
FFT code), the fix may have only been applied to one instance.

### Investigation Needed

- Compare test tab spectrum code with dashboard spectrum code
- Check if the frequency rolloff fix (possibly related to windowing,
  normalization, or FFT bin scaling) was applied to both instances
- Verify the UMIK-1 capture data itself is correct (flat spectrum in
  raw PCM) vs display-only issue

### Related

- F-099 (duplicate FFT pipeline — RESOLVED, but pattern may have recurred)
- F-101 (dashboard spectrum not consolidated with test tab)
- F-143 (spectrum frequency skew — previously RESOLVED)
- F-110 (BY-DESIGN: pink noise shows lower HF — but this is beyond that)
