## F-219: test_dashboard_screenshot visual regression — 10.33% pixel diff (OPEN)

**Filed:** 2026-03-30 (session 5)
**Severity:** Medium (test reliability — marginal threshold breach)
**Status:** Open
**Affects:** `src/web-ui/tests/integration/test_dashboard_screenshot.py` (or equivalent screenshot test)
**Found by:** QE (session 5 baseline audit)

### Description

The dashboard screenshot comparison test fails with a 10.33% pixel
difference against the reference snapshot, exceeding the 10.00% threshold
by 0.33 percentage points. This is a marginal failure — the visual change
may be intentional (e.g., from recent UI polish work) or an actual
regression.

### Root Cause

Unknown. Possible causes:
1. Accumulated minor UI changes since the reference snapshot was taken
   (font rendering, layout shifts, color adjustments from US-076 or
   subsequent visual work)
2. Environment difference (different font fallback, rendering engine
   version)
3. Actual visual regression

### Impact

- 1 of 7 E2E test failures in session 5 baseline
- Marginal failure (0.33% over threshold)
- Does not affect production functionality

### Fix

Investigation needed:
- Compare current screenshot against reference to identify the visual delta
- If changes are intentional: update the reference snapshot
- If regression: identify and fix the visual issue
- Consider whether the 10.00% threshold is appropriate or needs adjustment

### Related

US-076 (web UI color palette migration — most recent visual changes)
