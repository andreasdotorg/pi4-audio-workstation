# Task Register

Tasks are sub-story work items tracked below the story level. Each task belongs
to a parent story or is standalone infrastructure work. This file is the single
source of truth for items that are too granular for a user story but too important
to forget.

**Owner:** Project Manager
**Maintenance rule:** When any lab note, status update, or discussion surfaces a
new item that is not a full story, add it here with a source citation.

Status: `open` | `in-progress` | `blocked` | `done` | `wont-do`

---

## Active Tasks

| ID | Description | Owner | Status | Parent | Source | Notes |
|----|-------------|-------|--------|--------|--------|-------|
| TK-001 | UMIK-1 timing/sync research: does the USB audio stream provide a timing reference or sync signal? Does it have built-in latency measurement capability? | audio-engineer | done | standalone | user-stories.md:217, :226 (US-002 AC) | **DONE.** UMIK-1 uses adaptive USB clock mode (not asynchronous) — no timing reference. No built-in latency measurement capability. Neither needed: USBStreamer is master clock via PipeWire, and arrival-time detection compensates for constant capture delay. |
| TK-002 | Create CamillaDSP `active.yml` for systemd auto-start | change-manager | done | standalone | US-000 installation TODO #8, status.md line 67 | DONE. Symlink `/etc/camilladsp/active.yml` → `production/live.yml`. Survived reboot. |
| TK-003 | Test gpu_mem=128 for Mixxx (default ~76MB may be insufficient for OpenGL) | unassigned | open | US-006 | CLAUDE.md line 310, US-000 installation TODO #2 | Unblocked: TK-015 done (Mixxx launches). Next: test with gpu_mem=128 in config.txt and verify OpenGL rendering. |
| TK-004 | Decide: measurement pipeline uses REW or pure Python | architect | done | US-008 | CLAUDE.md line 309 | **DONE.** Owner decision (D-016): both. REW for exploratory/ad-hoc work, Python for the automation pipeline. Affects US-008 through US-013. |
| TK-005 | Investigate CamillaDSP websocket API for runtime filter hot-swapping | architect | in-progress | US-008/US-012 | CLAUDE.md line 313-314 | Architect + audio engineer analysis complete: `set_active()` produces clean mute ~50-200ms (file I/O dependent), no clicks/pops, safe for tap count changes, no pause needed. Superior to `systemctl restart` (avoids ALSA device race with PipeWire). Recommended for measurement pipeline. Pending architect response to close out. Pi validation still needed. |
| TK-006 | Flight case design: ventilation, cable routing, power distribution | unassigned | open | standalone (D-012) | CLAUDE.md line 317 | D-012 mandates active cooling. Blocks US-003 T4 (thermal test in flight case). Physical hardware task. |
| TK-007 | Disable cloud-init (~3.3s boot overhead) | unassigned | open | US-024 | US-000 installation TODO #7, status.md line 67 | Low priority. Permanent installation, not cloud. |
| TK-008 | Monitor pycamilladsp PyPI for Python 3.13 compatibility | unassigned | open | standalone | US-000 installation TODO #4 | Currently installed from GitHub. Watch for PyPI release. |
| TK-009 | PipeWire vs native JACK latency/stability comparison | unassigned | open | standalone | CLAUDE.md line 312 | Low priority -- PipeWire works. JACK alternative is speculative. |
| TK-010 | Live mode: evaluate whether shorter FIR filters would benefit chunksize 256 | unassigned | open | standalone | CLAUDE.md line 315-316 | Likely wont-do: US-001 proved 16k taps viable at chunksize 256 (19.25% CPU). |
| TK-011 | Install and test REW on Pi 4 ARM | unassigned | open | US-008 | CLAUDE.md line 308 | Java-based, should run on ARM. Useful for ad-hoc verification even if pipeline is pure Python. ~10 min task. |
| TK-012 | Verify CUPS/rpcbind port status | security-specialist | done | US-000a | US-000 installation TODO #5 | **DONE.** rpcbind and CUPS both disabled, not listening. Only SSH (22) exposed. F-011 confirmed resolved. |
| TK-013 | Lab notes T6: correct latency budget (old IEM bypass assumption) | technical-writer | done | US-002 | status.md line 67 | **DONE** (commit `5d46674`). Latency budget corrected for D-011 IEM passthrough. |
| TK-014 | Verify Reaper installation path convention (/home/ela/opt vs /opt) | unassigned | open | standalone | PM observation during US-000b review | Minor -- confirm whether ~/opt is intentional (Pi-Apps default) or should be /opt. |
| TK-015 | Mixxx smoke test: connect via RustDesk, launch Mixxx, verify it renders and plays audio | change-manager | done | US-029 | team-lead directive (UAT gap) | PARTIAL PASS. Launches on stock PREEMPT, 194 MB memory, clean kill. GUI rendering visually confirmed via wayvnc VNC session (2026-03-09). Audio output NOT YET VERIFIED -- pending TK-039. |
| TK-016 | Reaper smoke test: connect via RustDesk, launch Reaper, verify it runs with a project loaded | change-manager | done | US-030 | team-lead directive (UAT gap) | PARTIAL PASS on stock PREEMPT. Launches, 90s alive, clean kill. F-012: crashes on PREEMPT_RT (4x). GUI rendering not visually confirmed. |
| TK-017 | Hercules USB-MIDI functional test: actual DJ control, not just enumeration | unassigned | in-progress | US-005/US-029 | team-lead directive (UAT gap) | **A6 VALIDATED:** Hercules DJControl Mix Ultra detected as USB-MIDI on Pi. Worker building proper MIDI mapping using djercula reference. US-005 AC covers enumeration (now confirmed). TK-065 tracks the mapping work. |
| TK-018 | CamillaDSP stderr logging rule: all future test runs must use `2>/path/to/log` or run under systemd | unassigned | open | standalone | US-003 T3c analysis (lab notes line 527) | Monitoring gap: when CamillaDSP runs under sudo (not systemd), buffer underruns go to stderr only, not journal. Process improvement for all future tests. |
| TK-019 | US-003 T3a (real): DJ mode stability with Mixxx + Hercules (30 min) | unassigned | blocked | US-003 | US-003 AC line 251 | Blocked on: US-005 (Hercules MIDI), US-006 (Mixxx feasibility). TK-015 done (Mixxx launches). AC requires "Mixxx (2 decks, continuous playback)". |
| TK-020 | US-003 T3b (real): Live mode stability with Reaper + vocal input (30 min) | unassigned | blocked | US-003 | US-003 AC line 252 | Blocked on: Reaper project with backing tracks + FX chain (TK-016 done, Reaper launches on stock PREEMPT). AC requires "Reaper (8-track backing + FX)". On stock PREEMPT per D-015. |
| TK-021 | US-003 T3d: Production-config stability retest (8ch, ~34% load) with Reaper | unassigned | in-progress | US-003 | US-003 AC line 254 | **Pending AE/QE sign-off for closure.** Owner considers T3d satisfied by 30+ min continuous DJ playback with zero xruns (Run 8, 2026-03-10). Previously blocked on TK-020. AE and QE reviewing for formal closure. |
| TK-022 | F-012: Reaper hard kernel lockup on PREEMPT_RT — investigate and fix | unassigned | open | standalone | TK-016 Test B results, D-015 | Root cause confirmed identical to F-012 (V3D ABBA deadlock). Upstream fix validated with Mixxx (TK-055). Reaper retest pending -- will be covered by TK-039. Reopened per AD challenge. |
| TK-023 | Validate chrt -o 0 workaround: Reaper under SCHED_OTHER on PREEMPT_RT | change-manager | done | TK-022 | architect recommendation, D-015 | **FAIL.** Reaper under `chrt -o 0` (SCHED_OTHER) also caused hard kernel lockup on PREEMPT_RT. Rules out RT priority scheduling as the cause. |
| TK-024 | Script audit: map published test results to version-controlled scripts | quality-engineer | done | standalone | owner request (reproducibility) | **DONE.** US-001: fully traceable (run_benchmarks.sh + gen_configs.py + gen_dirac.py). US-002: fully traceable (measure_latency.py canonical, run_t2a.sh). US-003 T3b/T3c: fully traceable (run-stability-t3b/t3c.sh + monitors). **Gaps:** T3e has no script (ad-hoc), T3e raw data not in repo, T1d/T1e numbers wrong in status.md + user-stories.md (fixed). |
| TK-025 | Dead code cleanup: remove superseded scripts and fix misplacements | quality-engineer | done | standalone | owner request (repo structure), TK-024 findings | **DONE** (`19eac28`). 7 files removed, 1 config moved. Superseded: `test_i2_v2/v3/v4.py`, `test_i2_alsa.py`, `measure_latency_lowlat.py`. Duplicates removed: `scripts/test/stability-monitor.sh`, `scripts/test/xrun-monitor.sh`. Config moved: `test_8ch_loopback.yml` -> `configs/camilladsp/test/`. |
| TK-026 | scripts/ README: index of all scripts with purpose, test mapping, usage | technical-writer | done | standalone | owner request (repo structure) | **DONE.** `scripts/README.md` created. T1c/T1d/T1e description errors caught and fixed before commit. |
| TK-027 | results/ README: index of result files with test provenance | technical-writer | done | standalone | owner request (repo structure) | **DONE.** `results/README.md` created. T1e description error caught and fixed (was "8k taps, chunksize 256", corrected to "32k taps, chunksize 2048"). |
| TK-028 | configs/ README: index of test vs production configs | technical-writer | done | standalone | owner request (repo structure) | **DONE.** `configs/README.md` created. Documents production configs (`dj-pa.yml`, `live.yml`), test configs (T1a-T1e, T2a-T2b, passthrough, stability, loopback), PipeWire and WirePlumber configs. Notes deployment targets. |
| TK-029 | data/ README: index of raw test data with run provenance | technical-writer | done | standalone | owner request (repo structure) | **DONE.** `data/README.md` created. Documents T3b, T3c, T3e subdirectories with date, kernel, config, script, result status. Notes file sizes and schemas. T3e cyclictest_output.txt (24MB) gitignored. |
| TK-030 | Lab notes: add script/config/result cross-references | technical-writer | done | standalone | owner request (documentation linkage) | **DONE.** Reproducibility cross-references added to all 4 lab notes (US-001, US-002, US-003, US-028): script, config, and result paths for each test. |
| TK-031 | Repo layout section in top-level README | technical-writer | done | standalone | owner request (documentation linkage) | **DONE.** README repo layout section expanded with `scripts/`, `configs/`, `results/`, `data/`, `docs/` directory descriptions. |
| TK-032 | Write T3e reproducibility script (`scripts/stability/run-stability-t3e.sh`) | unassigned | open | US-003 | TK-024 gap finding | T3e was run with ad-hoc commands (no script). Write a script covering all 5 phases: stock baseline cyclictest, RT kernel install, regression test, validation, deploy. Must match the procedure documented in US-003-T3e-preempt-rt.md lab notes. |
| TK-033 | Copy T3e raw data from Pi to `data/US-003/T3e/` in repo | change-manager | done | US-003 | TK-024 gap finding | **DONE.** Committed as `67a8cb3`. Files: `stability_30min_rt.log` (3.4KB), `cyclictest_rt.txt` (14KB). Full `cyclictest_output.txt` (24MB) gitignored — histogram contains same stats. |
| TK-034 | T1c/T1d/T1e number discrepancy: verify all docs match lab notes | project-manager | done | US-001 | TK-024 gap finding | **DONE.** Lab notes (authoritative): T1c=19.25%, T1d=6.35%, T1e=6.61%. status.md and user-stories.md both had wrong numbers (T1c 20.43%, T1d 5.21%, T1e 10.39%). Fixed in both files. CLAUDE.md does not cite these numbers directly. |
| TK-035 | Install qt6-wayland for native Wayland rendering | change-manager | done | US-006 | owner VNC testing session (2026-03-09) | **DONE.** `qt6-wayland` installed, Mixxx confirmed running native Wayland (no longer XWayland). |
| TK-036 | Fix missing icons in Mixxx | change-manager | in-progress | US-006 | owner VNC testing session (2026-03-09) | Icon theme applied. `qt6-svg-plugins` installed during DJ test session. Pending Mixxx restart to confirm SVG icons render correctly. |
| TK-037 | Fix Reaper audio device access | change-manager | done | US-030 | owner VNC testing session (2026-03-09) | **DONE.** `pipewire-jack` installed, Reaper set to JACK backend. Owner verified audio device access working. |
| TK-038 | Configure fullscreen launch for Mixxx and Reaper | change-manager | done | standalone | owner VNC testing session (2026-03-09) | **DONE.** Owner confirmed Reaper launches fullscreen without decorations via VNC. Root cause: `ToggleFullscreen` fired on all 3 Reaper X11 windows (main + 2 menus), causing triple-toggle. Fix: `type="normal"` + `matchOnce="true"` + `serverDecoration="no"`. Reduced from 5 rules to 3. |
| TK-039 | End-to-end audio validation via VNC | unassigned | open | US-029/US-030 | owner VNC testing session (2026-03-09) | TK-015/TK-016 cannot be FULL PASS until audio is verified. **All blockers cleared:** ~~TK-040~~ done (owner VNC + AE sign-off), ~~TK-042~~ done (PASS), ~~TK-043~~ done, ~~TK-044~~ done (ALL PASS), ~~TK-047~~ done. **QE test plan (8 criteria + AD enhancements to 11):** (1) Mixxx produces audio on PA channels, (2) Reaper produces audio on PA channels, (3) CamillaDSP in signal path (FIR applied), (4) Correct channel routing per dj-pa.yml, (5) Correct channel routing per live.yml, (6) 0 xruns during 60s playback, (7) Peak levels > -40dBFS and < 0dBFS, (8) Owner confirms audio output via VNC session for both Mixxx and Reaper. **Test order:** reboot Pi first (TK-044), test Mixxx with dj-pa.yml, then Reaper with live.yml. Both modes tested separately. Signal monitoring via electronic capture on USBStreamer output channels. Criteria 1-7 are worker electronic checks; criterion 8 is mandatory owner confirmation per process rule. Must include Reaper stability check on `6.12.62+rpt-rpi-v8-rt` (covers TK-022 Reaper retest requirement per AD challenge). |
| TK-040 | Reaper JACK input: verify 8ch visible after port rename to ada8200-in | worker (pi-fixes) | done | US-030 | owner VNC validation (2026-03-09) | **DONE.** Owner confirmed via VNC: 2 UMIK-1 + 6 ADA8200 channels visible at I/O 8, all 10 channels (8 ADA8200 + 2 UMIK-1) visible at I/O 16. Audio engineer signed off — channel mapping correct. **AE notes for TK-045:** disable JACK auto-connect for production, manually route only ADA8200 ch 1-2 (vocal + spare) to Reaper inputs, do NOT connect UMIK-1 (measurement pipeline only), set Reaper to 8 inputs for production. |
| TK-041 | 64 phantom MIDI devices in Reaper + unwanted BLE MIDI | change-manager | done | US-030 | owner VNC validation (2026-03-09) | **DONE.** BT disabled and masked, snd-virmidi blacklisted. Root cause was ALSA sequencer layer (not JACK MIDI). JACK MIDI bridge clean (3 physical controllers only). BLE MIDI was BlueALSA artifact. Unblocks TK-045. |
| TK-042 | Verify CamillaDSP direct ALSA path not broken by pipewire-jack install | change-manager | done | US-001/US-002 | AD challenge (2026-03-09): pipewire-jack ALSA impact (HIGH) | **DONE. PASS.** `fuser` confirmed CamillaDSP holds exclusive `/dev/snd/pcmC*D*p` and `/dev/snd/pcmC*D*c`. `dpkg -L pipewire-jack` captured. Architect + audio engineer both signed off. Signal path clean, US-001/US-002 numbers remain valid. **AE finding:** PipeWire NOT holding USBStreamer capture device (pcmC3D0c) — `ada8200-in` node may not have opened capture. Likely explains TK-040 (Reaper not seeing USBStreamer inputs). Investigate after reboot. |
| TK-043 | Capture Pi package state for regression bisection | change-manager | done | standalone | AD challenge (2026-03-09): rollback documentation (HIGH) | **DONE.** 1698 packages captured via `dpkg --get-selections`. Committed to repo (`data/system-state/`). Baseline for regression bisection if TK-039 fails. |
| TK-044 | Reboot verification: all VNC session changes survive reboot | worker (pi-fixes) | done | standalone | QE review (2026-03-09) | **DONE. ALL 12 CHECKS PASS.** Clean reboot. CamillaDSP enabled and auto-started, wayvnc auto-started with password auth, RustDesk gone, firewall clean. All VNC session changes survived reboot. |
| TK-045 | Version-control Reaper configuration and project template | unassigned | open | US-030 | owner request (2026-03-09) | **Audio engineer scope defined.** TK-041 done (MIDI cleanup), PipeWire node naming done (`ada8200-in`, commit `8d9ec50`), TK-046 done (sample rate 48000), TK-040 done (owner confirmed channels). **No remaining blockers — waiting on owner Reaper setup.** **AE production notes from TK-040:** disable JACK auto-connect, manually route only ADA8200 ch 1-2 (vocal + spare) to Reaper inputs, do NOT connect UMIK-1 (measurement pipeline only), set Reaper to 8 inputs for production. Deliverables: (1) `configs/reaper/reaper-audio-settings.txt` — critical `reaper.ini` keys only (audiodev, jack_nch_in/out, jack_autoconnect, linux_audio_srate, MIDI enable/disable). Use `diff` to capture key names empirically. (2) `configs/reaper/live-template.RPP` — full project file with correct routing via `ada8200-in`/`ada8200-out` JACK port names. (3) Optionally `configs/reaper/dj-template.RPP`. **Workflow:** (1) ~~fix TK-046 sample rate~~ DONE, (2) owner configures Reaper (with AE production notes), (3) worker diffs reaper.ini + captures .RPP, (4) test fresh profile, (5) commit. **Caution:** `.RPP` media paths must be relative, not absolute. |
| TK-046 | Fix Reaper sample rate mismatch: 44100 Hz vs system 48000 Hz | change-manager | done | US-030 | TW lab note flag (2026-03-09) | **DONE.** Owner fixed via Reaper GUI during VNC session (2026-03-09). `reaper.ini` now has `linux_audio_srate=48000`. Verify survives reboot in TK-044. |
| TK-047 | Secure remote desktop: wayvnc password auth + RustDesk removal | security-specialist | done | US-000a | AD cumulative risk review (2026-03-09) | **DONE.** wayvnc auth: password=234269, config at `~/.config/wayvnc/config`, autostart via labwc. F-013 resolved. RustDesk removal: TK-048 done (service purged, firewall cleaned). F-014 resolved. Both security items complete. |
| TK-048 | Remove RustDesk from Pi (D-018 implementation) | change-manager | done | US-000a | D-018 implementation gap (2026-03-09) | **DONE.** RustDesk removed, verified clean. Service stopped/disabled, package purged, firewall rules for TCP 21118 and UDP 21116-21119 removed from `/etc/nftables.conf`, nftables reloaded, configs cleaned up. F-014 resolved. |
| TK-049 | Implement VNC TLS encryption for wayvnc | security-specialist | open | US-018 | Security reassessment after D-017 withdrawal (2026-03-09) | F-013 is PARTIALLY RESOLVED: password auth done, but VNC RFB uses 56-bit DES — adequate on private AP, inadequate when guest musicians join the network (US-018). **Blocker for US-018**, does NOT block TK-039 or current testing (no guest devices). Implementation: generate TLS cert, configure wayvnc with `--tls-cert` and `--tls-key`, update labwc autostart. VNC clients must support TLS (Remmina does). |
| TK-050 | Remove orphaned `/etc/udev/rules.d/99-uinput.rules` | worker (pi-fixes) | done | standalone | Review action item (2026-03-09) | **DONE.** uinput rule removed, permissions reverted to root:root 0600 via `udevadm trigger`. |
| TK-051 | Verify `dtoverlay=disable-bt` in `/boot/firmware/config.txt` | worker (pi-fixes) | done | standalone | Review action item (2026-03-09) | **DONE.** `dtoverlay=disable-bt` was absent — added to `[all]` section of config.txt. Takes effect on next reboot. Complements TK-041 systemd masking with kernel-level BT disable. |
| TK-052 | Version-control Pi-only configs: labwc rc.xml, CamillaDSP systemd drop-in, wayvnc autostart | worker (pi-fixes) | done | standalone | Review action item (2026-03-09) | **DONE.** Config files committed to repo in `b1f40b4`. Complements TK-045 (Reaper config). |
| TK-053 | Update stale RustDesk references in `docs/lab-notes/rustdesk-headless-setup.md` | technical-writer | done | standalone | Review action item (2026-03-09) | **DONE.** Deprecation notice added at top of lab note: RustDesk removed (D-018, TK-048), wayvnc is sole remote desktop, lab note retained as historical record. |
| TK-054 | DJ-A 15-minute stability test: Mixxx on PREEMPT_RT + V3D blacklisted + quantum 1024 + chunksize 2048 | unassigned | wont-do | US-003 | Architect + audio-engineer consensus (2026-03-09) | **wont-do.** Superseded by TK-055 (PASS). Hardware V3D GL is available on PREEMPT_RT `6.12.62+rpt-rpi-v8-rt` (D-022). Software rendering (pixman + llvmpipe + V3D blacklist) is no longer required. The DJ-A stability test was designed to validate software rendering viability — with hardware GL restored, this test is unnecessary. DJ-A confirmed via different path: PREEMPT_RT + hardware GL for both modes. |
| TK-055 | Apply upstream V3D RT fix and test PREEMPT_RT with hardware GL | team-lead | done | US-003 | raspberrypi/linux#7035, owner discovery (2026-03-09) | **DONE (PASS).** Upstream V3D fix (commit `09fb2c6f4093`, Melissa Wen / Igalia, merged 2025-10-28 by Phil Elwell) included in stock RPi kernel `6.12.62+rpt-rpi-v8-rt`. Kernel upgraded via `apt upgrade` (no custom build). Pi running `6.12.62+rpt-rpi-v8-rt` with hardware V3D GL for 37+ minutes — zero lockups (previous kernel `6.12.47+rpt-rpi-v8-rt` locked up in <2.5 min). **D-022 filed:** PREEMPT_RT + hardware V3D GL, software rendering no longer required. Supersedes D-021 clauses 2-4. F-012 RESOLVED, F-017 RESOLVED. TK-054 wont-do. DJ-A trivially viable — single kernel for both modes with hardware GL. |
| TK-056 | Security posture verification on Pi (SSH password auth, CamillaDSP port, rpcbind/CUPS status) | security-specialist | done | US-000a | Security specialist review (2026-03-10) | **DONE (ALL 5 CHECKS PASS).** No remediation required. (1) SSH password auth: already disabled. (2) CamillaDSP: port 1234 (not 5005 as originally assumed), bound to 127.0.0.1 only. (3) rpcbind: disabled (confirms TK-012). (4) CUPS: disabled (confirms TK-012). (5) Full port audit clean: SSH 22/tcp all interfaces, CamillaDSP 1234/tcp localhost, avahi 5353/udp all interfaces, wayvnc 5900/tcp when active (password auth). CLAUDE.md updated with corrected ports/firewall info. **Informational:** avahi on all interfaces (low risk, discovery only); VNC 5900 persisted without IP restriction (mitigated by password auth, TLS pending TK-049). |
| TK-057 | SETUP-MANUAL.md deprecation notice | technical-writer | open | standalone (D-023) | Owner directive (2026-03-10), PM observation | Add prominent deprecation notice to top of SETUP-MANUAL.md directing readers to ground truth hierarchy (D-023 clause 5). SETUP-MANUAL.md is obsolete and diverged from actual Pi state. Low priority -- tracking debt. |
| TK-058 | Version-control missing application configs (Mixxx soundconfig.xml, start-mixxx script, Reaper config) | change-manager | done | standalone (D-023) | TK-039 Phase 1 finding, F-021, D-023 clause 1 | **DONE.** TK-058.1 (`4498f99`) and TK-058.2 (`467cc1e`) both complete. Reaper config tracked separately in TK-045. |
| TK-058.1 | Capture Mixxx soundconfig.xml from Pi | pi-recovery-worker | done | TK-058 (D-023) | Architect decomposition (2026-03-10) | **DONE.** `configs/mixxx/soundconfig.xml` committed `4498f99`. HP channel `channel="4"` committed as-is per AE assessment (PortAudio internal offset). Empirical verification during DJ test. |
| TK-058.2 | Create start-mixxx.sh launch script | pi-recovery-worker | done | TK-058 (D-023) | Architect decomposition (2026-03-10), D-026, F-022 | **DONE.** `scripts/launch/start-mixxx.sh` created with D-026 readiness probe + `pw-jack` wrapper. Architect approved. Committed `467cc1e`. |
| TK-059 | Create deploy script or documented deploy procedure | pi-recovery-worker | done | standalone (D-023) | D-023 clause 3, owner mandate, architect decomposition (2026-03-10) | **DONE.** `scripts/deploy/deploy.sh` committed `96e45f5`. Explicit file manifest, D-023 clean-tree check, --dry-run, --mode dj/live, --reboot. soundconfig.xml IS in manifest (line 84) — will deploy if file exists in repo. |
| TK-060 | Convert TK-039 test procedure to executable script | quality-engineer | done | TK-039 (D-023) | D-023 clause 2, owner mandate | **DONE.** QE reviewed, 6 findings (R-1 through R-6) applied in `8ab9aa9`. TP-001 approved with QE + AE + AD sign-off (`ac43007`). D-024 satisfied. |
| TK-061 | Configure libjack alternative to prefer PipeWire | pi-recovery-worker | wont-do | standalone | Architect + AE recommendation (2026-03-10), F-021 root cause, architect decomposition (2026-03-10) | **wont-do (D-027).** `update-alternatives` approach blocked by `ldconfig` — Debian's dynamic linker cache resolves `libjack.so.0` from the package-owned path regardless of alternatives. Three attempts (S-005 path mismatch, S-006 partial, S-007 dpkg-divert) all failed. All changes rolled back, Pi at Cycle 1 baseline. **F-021 mitigation:** `pw-jack` wrapper in `scripts/launch/start-mixxx.sh` (TK-058.2, committed `467cc1e`) is the permanent solution. |
| TK-062 | Add -24dB speaker trim as Gain filters to CamillaDSP configs | unassigned | open | standalone | AE + architect recommendation (2026-03-10), owner priority #2 (2026-03-10) | **REVIVED PRIORITY.** Owner requested as priority #2 after DJ test PASS. Add a shared `speaker_trim` Gain filter definition (-24dB) applied to channels 0-3 (speaker outputs) in both `dj-pa.yml` and `live.yml`. Architect recommends one shared filter definition referenced by all 4 speaker channels. **-24dB is a testing/office value -- MUST be commented as non-production.** Production value will be set during venue gain structure calibration per D-009. Deployment step per D-025. |
| TK-063 | Web-based monitoring dashboard for Pi audio system | unassigned | open | US-020 (D-020) | Owner request (2026-03-10) | **Preliminary.** Real-time browser dashboard for monitoring PipeWire state, CamillaDSP processing load/xruns, scheduling priorities (FIFO vs OTHER), buffer levels, temperature. Builds on D-020 PoC (validated `29722c0`): FastAPI + binary WS PCM + browser FFT + pycamilladsp meters. Scope TBD — needs architect decomposition and PO story refinement. D-020 Stage 2+ work. |
| TK-064 | Track Loopback buffer finding from DJ test | architect | done | standalone | DJ test Run 1-4 finding (2026-03-10), commit `f6e941b` | **DONE.** Mixxx crashed on DJ test runs 1-4 due to PipeWire Loopback node buffer being undersized for DJ quantum 1024. Root cause: `25-loopback-8ch.conf` buffer settings insufficient when PipeWire graph runs at quantum 1024 (DJ mode). Fix committed `f6e941b` (increase Loopback buffer). Documented as design discovery in `docs/architecture/rt-audio-stack.md` section 5 (The Loopback Buffer Discovery). F-023 ID assigned to GPU contention defect (separate issue). |
| TK-065 | Hercules DJControl Mix Ultra MIDI mapping for Mixxx | pi-recovery-worker | in-progress | US-005/US-029 | Owner priority (2026-03-10), TK-017 finding | **In progress.** Worker building proper MIDI mapping using djercula reference. Hercules DJControl Mix Ultra confirmed as USB-MIDI on Pi (TK-017 A6 validated). Deliverable: Mixxx-compatible MIDI mapping XML enabling deck control, EQ, crossfader, jog wheels. Dependency: TK-017 (USB-MIDI enumeration confirmed). |
| TK-066 | Full version upgrade: system packages + PipeWire 1.4.9 + Mixxx 2.5.4 + Reaper 7.64 | pi-recovery-worker | done | standalone | Orchestrator directive (2026-03-10) | **DONE (3/4).** Completed: (1) system packages: 148 upgraded (security patches included), (2) PipeWire 1.4.2 -> 1.4.9 (trixie-backports), (3) Reaper 7.31 -> 7.64. **Mixxx 2.5.0 -> 2.5.4 BLOCKED:** requires Qt 6.9 (Trixie has 6.8.2), pulling Qt from sid too risky. Mixxx stays at 2.5.0. Post-reboot verification: ALL PASS (RT scheduling, CamillaDSP, JACK routing, controllers). CLAUDE.md and README.md version numbers updated. |
| TK-067 | Reaper-always-on feasibility test (7-phase plan) | pi-recovery-worker | in-progress | US-029/US-030 | Architect test plan (2026-03-10), owner approved | **In progress (suspended for day). Rollback point: `c89047e`.** Tests whether Reaper can run continuously alongside Mixxx in DJ mode, eliminating the need for mode-switching. **Phase 1a PARTIAL PASS:** Reaper idle CPU = 4.4% with hardware GL (well under 40% threshold). **F-023 FOUND:** dual V3D GL apps (Mixxx + Reaper) cause GPU contention stall on Pi 4B. Mitigation: `LIBGL_ALWAYS_SOFTWARE=1` for Reaper (architect recommendation, pending test). CamillaDSP stall/resume in logs = **HYPOTHESIS (pending verification)** that this is normal idle behavior when no audio is flowing -- needs evidence: (a) does stalling stop when audio flows? (b) could it mask a USBStreamer issue? (c) was it present before this session? Reaper NOT the cause of stalls -- stalling predated Reaper launch, persisted after kill. Phase 2 not started. **Next session:** relaunch Reaper with `LIBGL_ALWAYS_SOFTWARE=1 pw-jack reaper`, re-run P1a with llvmpipe, proceed to Phase 2 (chunksize 256 gating test). Architect 7-phase plan: **(1) Reaper baseline CPU** **(2) Quantum 1024 / chunksize 256 GATING TEST** **(2.5) PipeWire graph stability** **(3) Dual-app coexistence** **(4) JACK port routing** **(5) Connect/disconnect resilience** **(6) APCmini as Reaper mixer** **(7) Unified CamillaDSP config.** |
| TK-068 | Nix flake dev environment | flake-writer | done | standalone | Overnight batch (2026-03-11) | **DONE.** Deliverables: `flake.nix` (Python 3.13 dev shell, multi-platform) + `.gitignore` (updated with .venv/ and result). Re-spawned on main after worktree auto-cleanup. Uncommitted. Pending architect review. |
| TK-069 | MIDI system controller daemon | midi-daemon-worker | done | US-036 | Owner request (2026-03-10), overnight batch (2026-03-11) | **DONE.** Deliverables: `scripts/midi/midi-system-controller.py` (348 lines, full proxy architecture), `configs/midi/apcmini-mk2.yml` (button mapping config), `configs/systemd/user/midi-system-controller.service` (systemd service), `scripts/midi/test_midi_daemon.py` (60 tests, all pass), `scripts/midi/README.md`. Bug fix applied (confirmation timeout cleanup). Owner request: "launch mixxx, toggle reaper, stats, mixxx view, all from the MIDI controller." Uncommitted. Pending architect + QE review. |
| TK-070 | D-020 monitoring web UI Stage 1 | d020-webui-worker | done | US-020 (D-020) | TK-063, overnight batch (2026-03-11) | **DONE.** Deliverables: `scripts/web-ui/app/` (FastAPI SPA with monitoring + system views, 13 files), `scripts/web-ui/test_server.py` (71 tests, all pass), `scripts/web-ui/README.md`. Bug fix applied (scenario E missing chunksize). Builds on validated PoC (`29722c0`). Advances TK-063 (owner-requested web dashboard). Uncommitted. Pending architect review. |
| TK-071 | Automated room correction pipeline | room-correction-worker | done | US-008/US-009/US-010/US-011/US-012/US-013 | CLAUDE.md next steps, overnight batch (2026-03-11) | **DONE.** Deliverables: `scripts/room-correction/room_correction/` (13 modules: sweep, deconvolution, correction, crossover, combine, export, verify), `scripts/room-correction/mock/` (room simulator + config), `scripts/room-correction/runner.py` (CLI with --test mode), `configs/room-correction/` (default profile + venue template), README. All verification tests pass (D-009 compliant). Largest overnight deliverable — the project's major unstarted pipeline. Uncommitted. Pending architect + AE review for DSP correctness. |
| TK-072 | Investigate pipewire-force-quantum.service not firing on boot | unassigned | open | standalone | Morning briefing audit (2026-03-11), rt-audio-stack.md line 297 | **Filed.** The systemd oneshot service (`configs/systemd/user/pipewire-force-quantum.service`) that runs `pw-metadata -n settings 0 clock.force-quantum` after PipeWire starts may not be firing reliably on boot. DJ mode requires quantum 1024 set at runtime — if this service fails, quantum defaults to 256 (live mode). Needs investigation: (a) is the service enabled? (b) does it have correct After= dependency on pipewire.service? (c) does `pw-metadata` succeed when the service runs (PipeWire may not be fully ready)? (d) check `systemctl --user status pipewire-force-quantum.service` on Pi. Related: F-018 (persistence of audio config across reboot). |
| TK-073 | US-037: Create e2e test directory + conftest | playwright-worker | done | US-037 | US-037 AC (test directory), 2026-03-11 | **DONE.** Created `scripts/web-ui/tests/e2e/` directory with `__init__.py`. Existing `test_server.py` stays in place. Created `tests/e2e/conftest.py` with fixtures (mock_server, page, frozen_page, pi_url), markers (browser, e2e, destructive, slow), CLI options (--destructive, --update-snapshots). Committed `6e8d097`, fixes `f949919`. |
| TK-074 | US-037: Playwright conftest fixtures + markers | playwright-worker | done | US-037 | US-037 AC (mock backend fixture, browser fixture, markers), 2026-03-11 | **DONE.** `mock_server` fixture: subprocess uvicorn on free port, session-scoped, 5s teardown with force-kill. `page` fixture: overrides pytest-playwright's page with console error capture + auto-navigation. `frozen_page`: deterministic visual regression (fixed viewport 1280x720, WebSocket wait). Markers registered. No production code changes. Committed `6e8d097`, fixes `f949919`. |
| TK-075 | US-037: Playwright functional tests (4 files) | playwright-worker | done | US-037 | US-037 AC (example functional tests), 2026-03-11 | **DONE.** 4 test files (expanded from 3): `test_navigation.py` (6 tests), `test_monitor_view.py` (7 tests), `test_system_view.py` (10 tests), `test_stub_views.py` (6 tests — Measure + MIDI stubs). All use `pytestmark = pytest.mark.browser`. `re.compile()` for Playwright Python class regex. 29 browser tests total. Committed `6e8d097`, fixes `f949919`. |
| TK-076 | US-037: E2e scaffolding + example test | playwright-worker | done | US-037 | US-037 AC (e2e support), 2026-03-11 | **DONE.** `PI_AUDIO_URL` env var support in conftest (`pi_url` fixture, skips if unset). `@pytest.mark.destructive` skipped unless `--destructive` CLI flag. `test_monitor_e2e.py`: 1 smoke test (page loads, title correct). `test_visual_regression.py`: manual PNG comparison (pytest-playwright `to_have_screenshot()` unavailable in 1.57.0). E2e against real Pi deferred (needs CM OBSERVE session). Committed `6e8d097`, fixes `f949919`. |
| TK-077 | Fix missing default_profile.yml (runner --test crash) | dsp-fixes-worker | done | US-008/US-012 | Architect review of TK-071, 2026-03-11 | **DONE** (`d36721c`). `default_profile.yml` already existed; `transition_freq` updated to 500Hz as part of TK-084 fix. Runner `--test` mode now works. |
| TK-078 | US-008 gaps: UMIK-1 calibration parser + multi-position support | unassigned | open | US-008 | Architect AC mapping, 2026-03-11 | **Filed.** US-008 at ~40% coverage. Gaps: (1) ~~UMIK-1 calibration file parser~~ **resolved by TK-085** (`9720659`). (2) Multi-position measurement support (3-5 positions, spatial averaging in frequency domain) — see also TK-082 (M-1: CRITICAL, averaging not implemented, AC correction needed). (3) PipeWire/JACK audio routing for sweep output and UMIK-1 input. (4) Error handling (clipping detection, low signal level). Items 2-4 require Pi access. |
| TK-079 | US-009 gaps: edge cases + CamillaDSP delay integration | unassigned | open | US-009 | Architect AC mapping, 2026-03-11 | **Filed.** US-009 at ~70% coverage. Core time alignment algorithm correct. Gaps: (1) Edge case handling (single-speaker systems, identical arrival times). (2) CamillaDSP delay parameter integration (generate delay values compatible with CamillaDSP YAML). (3) Unit tests for edge cases. Partially doable without Pi. |
| TK-080 | US-010 gap: ported subwoofer subsonic protection filter | worker-c | done | US-010 | Architect AC mapping, 2026-03-11 | **DONE** (`0097f7a`). Ported subwoofer subsonic protection filter implemented. M-3 (transition_freq) also resolved by TK-084. US-010 gaps closed. AE approved. |
| TK-081 | US-011 gap: bandpass crossover for 3-way systems | unassigned | open | US-011 | Architect AC mapping, 2026-03-11 | **Filed.** US-011 at ~85% coverage. Gap: bandpass filter generation for 3-way crossover mid driver. Deferred by design (3-way is Phase 2, DJ mode only per US-011b note). Low priority — 2-way is the primary topology. Pure software when needed. |
| TK-082 | **M-1 CRITICAL:** Multi-position measurement + spatial averaging | unassigned | open | US-008 | AE DSP review of TK-071, 2026-03-11 | **Filed (CRITICAL). UNBLOCKED** — US-008 AC corrected by PO (commit `e304eea`): magnitude average in dB, phase from Position 1, minimum-phase via Hilbert transform. Multi-position measurement and spatial averaging not implemented in TK-071. Cross-ref TK-078. Partially doable without Pi (averaging algorithm), but multi-position recording needs Pi + UMIK-1. |
| TK-083 | M-2: Fix or remove inverse sweep dead code (DSP normalization bug) | dsp-fixes-worker | done | US-008 | AE DSP review of TK-071, 2026-03-11 | **DONE** (`d36721c`). Dead code removed: `generate_inverse_sweep()`, `deconvolve_with_inverse()` functions, 3 associated tests, and dead call site in `runner.py`. |
| TK-084 | M-3: Windowing transition_freq 300Hz -> 500Hz | dsp-fixes-worker | done | US-010 | AE DSP review of TK-071, 2026-03-11 | **DONE** (`d36721c`). `transition_freq` updated from 300Hz to 500Hz in `dsp_utils.py` and `default_profile.yml`. Cross-ref TK-080 (M-3 portion resolved). |
| TK-085 | M-4: Integrate UMIK-1 calibration into pipeline runner | worker-c | done | US-008 | AE DSP review of TK-071, 2026-03-11 | **DONE** (`9720659`). UMIK-1 calibration wired into pipeline runner Stage 2. AE approved. Cross-ref TK-078. |
| TK-086 | Pre-existing test bug: test_fade_out_ends_at_zero | unassigned | open | US-008 | DSP fixes worker discovery, 2026-03-11 | **Filed (LOW).** `test_fade_out_ends_at_zero` fails — discrete Hann window at N-1 samples gives 0.00616, not 0.0. Pre-existing in TK-071, unrelated to any fix. Fix: either relax assertion tolerance (`np.isclose` with atol) or use N+1 Hann window points. Pure software, trivial. |
| TK-087 | QE-F1: Add numpy to Makefile install-test-deps | unassigned | open | US-037 | QE review of TK-070, 2026-03-11 | **Filed.** `scripts/web-ui/Makefile` `install-test-deps` target missing numpy. `test_visual_regression.py` requires numpy for PNG pixel-diff comparison. One-line fix: add `numpy` to the pip install line. Trivial. |
| TK-088 | F-UX-001: Monitor meter signal-flow grouping (capture = cyan) | unassigned | open | TK-070 (D-020) | UX review of D-020 Stage 1, 2026-03-11 | **Filed (HIGH — D-020 spec deviation).** D-020 section 10 specifies three functional groups: CAPTURE (cyan) \| PA SENDS (green/yellow/red) \| MONITOR SENDS (green/yellow/red). Current implementation uses flat Capture/Playback split with identical green/yellow/red for both. Fix: capture meter bars use cyan color scheme, distinct from playback. Affects `monitor.js` canvas rendering + `style.css`. Pure software. |
| TK-089 | F-UX-003: Auto-hide capture channels 3-8 when no signal | unassigned | open | TK-070 (D-020) | UX review of D-020 Stage 1, 2026-03-11 | **Filed (MEDIUM — D-020 spec deviation).** D-020 spec: "Meters for capture channels 3-8 are hidden by default. Show automatically when peak exceeds -60 dBFS." Current implementation always shows all 8 capture meters. In DJ mode (scenario A), channels 2-7 show -120dBFS — 6 dead meters cluttering display. Fix: JS logic to show/hide capture meters based on peak threshold. Affects `monitor.js`. Pure software. |
| TK-090 | F-UX-005: dB scale text labels on meter grid lines | unassigned | open | TK-070 (D-020) | UX review of D-020 Stage 1, 2026-03-11 | **Filed (MEDIUM — professional metering standard).** CSS draws faint horizontal lines at -48, -36, -24, -12, -6 dB but no text labels. At stage distance (2-3m from kiosk), grid lines are invisible. Fix: add dB text labels to grid lines in canvas rendering. Affects `monitor.js`. Pure software, small effort. |
| TK-091 | F-UX-006: 3-second clip indicator latch per D-020 spec | unassigned | open | TK-070 (D-020) | UX review of D-020 Stage 1, 2026-03-11 | **Filed (HIGH — D-020 spec deviation).** D-020 spec: "Clip indicators: meter bar turns red when peak exceeds -1 dBFS. Stays red for 3 seconds." Current: colors above -6dB via gradient, peak hold 1.5s only. No distinct clip latch. Fix: add per-channel clip state tracking with 3-second timer, distinct visual indicator (e.g., red bar top or channel label highlight). Affects `monitor.js`. Pure software. |
| TK-092 | Clickable Measure view prototype (static HTML/CSS) | unassigned | open | TK-070 (D-020) | PM/PO/UX priority recommendation, 2026-03-11 | **Filed (post-demo).** Static clickable prototype (~400 lines) rendering UX wireframes for all 5 Measure sub-tabs (Speakers, Calibrate, Measure, Results, Presets) + deploy safety flow. No WebSocket, no backend — button-linked navigation between pre-rendered states. Lets owner validate UX design before committing to full ~1500-2000 line mock implementation. Triggered by owner feedback from Stage 1 walkthrough. Pure software. |

---

## Completed Tasks

Items moved here when resolved. Preserves audit trail.

| ID | Description | Owner | Status | Parent | Source | Resolution |
|----|-------------|-------|--------|--------|--------|------------|
| TK-100 | Verify snd-aloop persists across reboot with index=10 | change-manager | done | US-000 | US-000 installation TODO #1 | DONE in T6. Loopback at card 10 after reboot. |
| TK-101 | Fix USBStreamer 4-channel capture (PipeWire sees only 4ch) | change-manager | done | US-028 | US-000 installation TODO #3 | DONE in T5 + US-028. Explicit PipeWire config with 8 channels. |
| TK-102 | Install RTKit for PipeWire real-time priority | change-manager | done | US-000b | US-000 installation TODO #6 | DONE in US-000b. RTKit installed, PipeWire FIFO rtprio 83-88. |
| TK-103 | PipeWire TS scheduling (running timeshare, not FIFO) | change-manager | done | US-000b | US-001 lab notes line 369, status.md | RESOLVED by US-000b. PipeWire now runs FIFO rtprio 83-88. |
| TK-104 | PipeWire Loopback stereo-only (BLOCKER for production 8ch routing) | change-manager | done | US-028 | US-003 stability tests line 171 | RESOLVED by US-028. Custom PipeWire profile + WirePlumber rules deployed. |
| TK-105 | PREEMPT_RT kernel package availability check | change-manager | done | US-003 | CLAUDE.md line 311 | DONE. `linux-image-6.12.47+rpt-rpi-v8-rt` available in Trixie repos. Installed in T3e Phase 1-2. |
| TK-106 | Fix CamillaDSP ALSA path (hw:3,0 -> hw:USBStreamer,0) | change-manager | done | US-000b | US-000b lab notes line 434-438 | DONE in US-000b T7. Stable ALSA name prevents USB renumbering failures. |

---

## Notes

- Tasks TK-001 through TK-076 are active. **US-037 (Playwright browser test scaffolding) decomposed (2026-03-11):** TK-073 (test directory consolidation), TK-074 (conftest fixtures + markers), TK-075 (3 functional test files), TK-076 (e2e scaffolding + example). Single worker, sequential build, no Pi needed for local/mock mode. Architecture defaults: no production code changes, uvicorn in background thread, sys.path.insert in conftest. **DJ mode validation PASS (2026-03-10, Run 8).** Recovery deploy cycles 1 and 3 complete. TK-066 done (3/4 upgrades, Mixxx blocked on Qt 6.9). **Current priorities:** TK-067 (Reaper-always-on test, suspended, F-023 found), TK-065 (Hercules mapping, in-progress), TK-062 (speaker gain, revived). **Overnight batch (2026-03-11):** TK-068 (flake.nix), TK-069 (MIDI daemon, US-036), TK-070 (web UI Stage 1), TK-071 (room correction pipeline). TK-021 pending AE/QE sign-off for T3d closure. Done in place: TK-001, TK-002, TK-004, TK-012, TK-013, TK-015 (PARTIAL -- audio pending TK-039), TK-016, TK-023 (FAIL), TK-024, TK-025, TK-026, TK-027, TK-028, TK-029, TK-030, TK-031, TK-033, TK-034, TK-035, TK-037, TK-038, TK-040, TK-041, TK-042, TK-043, TK-044, TK-046, TK-047, TK-048, TK-050, TK-051, TK-052, **TK-055 DONE (PASS)**, **TK-056 DONE** (all 5 checks PASS), TK-058 DONE, TK-058.1 DONE, TK-058.2 DONE, TK-059 DONE, TK-060 DONE, **TK-066 DONE (3/4)**. TK-054/TK-061 wont-do. TK-005/TK-017/TK-036 in-progress. TK-049 blocks US-018 (VNC TLS), does NOT block TK-039. TK-045 open, waiting on owner Reaper setup. TK-053 done. TK-100+ are completed (numbering gap is intentional for clarity).
- TK-024 through TK-034 are the reproducibility/repo-structure audit (owner request). All done except **TK-032** (T3e reproducibility script). TK-024 (QE audit), TK-025 (dead code), TK-026-029 (READMEs), TK-030 (lab notes cross-refs), TK-031 (repo layout), TK-033 (T3e raw data), TK-034 (number fix) -- all complete.
- US-003 T3e complete (PREEMPT_RT installed + validated, 30-min 0 xruns, cyclictest max 209us). F-012 RESOLVED (D-022, upstream fix in `6.12.62+rpt-rpi-v8-rt`). D-015 superseded by D-022 -- PREEMPT_RT + hardware V3D GL for production.
- UAT stories created by product owner: US-029 (DJ UAT), US-030 (Live UAT), US-031 (Full Rehearsal). TK-015/016/017 are prerequisite smoke tests.
- The "T3b" in lab notes `US-003-stability-tests.md` is synthetic (CamillaDSP + aplay). The real T3b per AC requires Reaper load (TK-020).
- TK-035 through TK-041 are from the owner's VNC testing session (2026-03-09). TK-040 (USBStreamer 8ch in Reaper) blocks TK-039 (end-to-end audio validation), which blocks TK-015/TK-016 FULL PASS.
- **PROCESS RULE (2026-03-09):** Any task requiring owner-visible verification (GUI rendering, audio output, MIDI response) must NOT be closed until the owner confirms the result via VNC or direct observation. Worker testing is necessary but not sufficient. This rule was established after TK-040 was prematurely closed based on worker verification alone — the owner never saw the expected 8 channels in Reaper because the worker killed the application before owner could check.
- **AD challenge batch (2026-03-09):** 6 challenges filed (3 HIGH, 2 MEDIUM, 1 LOW). HIGH items: (1) A6 must NOT be marked validated until TK-017 produces `aseqdump` evidence — `lsusb` is enumeration only, (2) TK-042 filed: verify pipewire-jack didn't break CamillaDSP direct ALSA path (benchmark invalidation risk), (3) TK-043 filed: capture package state for rollback/bisection. MEDIUM: D-019 needs fallback controller contingency, qt6-wayland Mixxx rendering included in TK-039 scope. LOW: D-016 REW ARM compatibility — don't invest in TK-011 until confirmed.
- **QE review (2026-03-09):** Validation gap analysis for TK-035 through TK-041. Findings: TK-035 adequate, TK-036 incomplete (needs owner visual), TK-037 lab note stale (says "awaiting" but task is done), TK-040 contradictory status (resolved by reverting to in-progress). TK-044 filed: coordinated reboot verification (11 items) must complete before TK-039. TK-039 test plan: 7 acceptance criteria covering both Mixxx (dj-pa.yml) and Reaper (live.yml) modes, channel routing, xrun count, signal levels. Reboot-first test order. Evidence gaps: no `pw-jack jack_lsp` output captured for TK-037/TK-040 session, Reaper JACK persistence unverified.
- **PROCESS RULE L-004 (2026-03-09):** CM handles git operations and Pi access lock tracking ONLY. All SSH commands on the Pi must be executed by workers. CM grants the access lock to a worker and tracks it, but does not execute Pi commands. See `.claude/team/lessons-learned.md` L-004.
- **DJ-A / DJ-B decision path: RESOLVED (2026-03-10).** D-022 collapses the decision tree. PREEMPT_RT + hardware V3D GL for both DJ and live modes. No dual-kernel strategy needed. No software rendering overhead. Original DJ-A (RT + V3D blacklisted) and DJ-B (stock PREEMPT for DJ) are both superseded by the simpler configuration: RT + hardware GL for everything.
- **UPSTREAM V3D FIX FOUND (2026-03-09 late evening).** `raspberrypi/linux#7035`: Melissa Wen (Igalia, DRM/V3D maintainer) provided a patch (`0001-drm-v3d-create-a-dedicated-lock-for-dma-fence.patch`) that fixes the exact ABBA deadlock we diagnosed in F-012. Bisected to commit `5a72e3ae00ec` (introduced 2025-07-25). Confirmed working by 2 independent reporters (Pi 4B + Pi 5, kernels 6.12.44-rt and 6.12.47-rt). Third reporter (MmAaXx500) provided kernel trace on OUR EXACT kernel (`6.12.47+rpt-rpi-v8-rt`, Pi 4B Rev 1.5) showing `BUG: scheduling while atomic: irq/46-v3d` in `v3d_job_update_stats` — matches our root cause analysis precisely. **TK-055 filed: apply patch and test RT + hardware GL. If it works, D-021 software rendering requirement is eliminated.** Evaluate TK-055 BEFORE TK-054.
- **Session crash (2026-03-09 ~22:48 CET):** Claude Code orchestration crashed. Pi was fine (running stably after Test 5 watchdog recovery). All team agents lost. No uncommitted code was lost (1ff916c). TK-054 had not yet started execution. Team state recovered from `~/.claude/teams/wondrous-riding-lerdorf/` inbox files.
- **Session 2026-03-10:** TK-055 completed (PASS). Upstream V3D RT fix (Melissa Wen / Igalia, commit `09fb2c6f4093`) confirmed merged into RPi kernel `6.12.62+rpt-rpi-v8-rt` (merged 2025-10-28 by Phil Elwell). Kernel upgraded via stock `apt upgrade` — no custom build. Pi running `6.12.62+rpt-rpi-v8-rt` with hardware V3D GL for 37+ minutes, zero lockups (previous `6.12.47+rpt-rpi-v8-rt` locked up in <2.5 min). **D-022 filed:** PREEMPT_RT + hardware V3D GL, software rendering no longer required. Supersedes D-021 clauses 2-4. F-012 RESOLVED, F-017 RESOLVED. TK-054 wont-do (hardware GL makes software rendering test unnecessary). DJ-A / DJ-B decision tree collapsed: PREEMPT_RT + hardware GL for everything. Formal 15-min monitored test PASS (5 checks, 0 kernel errors, peak 70.6°C, 1h30m total uptime).
- **F-020 RESOLVED (workaround, 2026-03-10).** systemd user service drop-in deployed at `~/.config/systemd/user/pipewire.service.d/override.conf` — `CPUSchedulingPolicy=fifo` + `CPUSchedulingPriority=88`. PipeWire confirmed at FIFO/88 after reboot. Config in repo (commit `536f631`). T3d and TK-039 now unblocked.
- **D-023 filed (2026-03-10).** Owner mandate: reproducible test protocol. All configs under version control, executable test scripts, deploy-and-reboot before test, git commit hash in lab notes. **TK-039 BLOCKED** until TK-058 (missing configs), TK-059 (deploy script), TK-060 (test script) are complete. TK-057 (SETUP-MANUAL.md deprecation) is low-priority tracking debt. **SETUP-MANUAL.md is OBSOLETE** -- ground truth hierarchy: CLAUDE.md > Pi itself > configs/ > docs/project/ > SETUP-MANUAL.md (last).
- **D-024 filed (2026-03-10).** Testing DoD requires QE approval of both test protocol (before execution) and test execution record (after completion). Applies to TK-039, T3d, all validation tasks.
- **D-025 filed (2026-03-10).** Deployment sequencing: one change at a time. Deploy -> reboot -> gate check -> verify -> commit evidence -> next change. No batching. Permanent process.
- **D-026 filed (2026-03-10).** Mixxx launch script must include PipeWire JACK bridge readiness probe (`pw-jack jack_lsp` retry loop with timeout). Prevents F-021 on cold boot.
- **F-021 filed (2026-03-10).** Mixxx silently falls back from JACK to ALSA. Root cause: `libjack.so.0` resolves to JACK2, no `pw-jack` wrapper. Fix: TK-058 (immediate) + TK-061 (permanent).
- **F-022 filed (2026-03-10).** Mixxx auto-launches on boot without `pw-jack`. Triggers F-021 on every reboot. Fix: versioned launch script with PipeWire readiness probe (D-026).
- **Deployment order (per D-025, after baseline confirmed):** ~~(1) TK-061 libjack alternatives~~ wont-do (D-027, ldconfig blocks update-alternatives). F-021 mitigated by start-mixxx.sh (pw-jack wrapper). Remaining: (1) TK-062 -24dB speaker trim (deferred, not required for DJ test). Each step is its own commit/deploy/reboot/verify cycle.
- **Recovery deployment plan (2026-03-10, architect decomposition).** ALL STOP lifted. **Cycle 1: PASS** (S-004, `1f0ce53`, 43 files, DJ mode baseline). **Cycle 2: wont-do** (S-005 halted, S-006 partial, S-007 rollback — TK-061/D-027). **Cycle 3: PASS** (`f6e941b`, Loopback buffer fix for DJ quantum 1024). **DJ test in progress** — Run 5 after Loopback fix. Runs 1-4 failed due to undersized Loopback buffer (Mixxx crash). Multiple test script fixes also landed (R-7 through R-11: `70a14e3`, `d730a56`, `863025b`, `6624720`, `f90d355`).
- **TK-063 filed (2026-03-10).** Owner request: web-based monitoring dashboard. Builds on D-020 PoC. Preliminary — needs architect decomposition.
- **TK-064 filed (2026-03-10).** Loopback buffer finding from DJ test runs 1-4. Mixxx crashed due to undersized PipeWire Loopback buffer at DJ quantum 1024. Fix: `f6e941b`. Documented as design discovery in `docs/architecture/rt-audio-stack.md` section 5 (TK-064 subsection). F-023 ID now assigned to GPU contention defect (see F-023 in defects.md). Loopback finding is resolved — no separate F-number needed.
- **DJ MODE VALIDATION PASS (2026-03-10, Run 8).** ALL CRITERIA PASS after Loopback buffer fix (`f6e941b`) and HP channel fix (`5e7766e`). Runs 1-4 failed (Loopback buffer), Run 5+ after fix. DJ test complete.
- **TK-065 filed (2026-03-10).** Hercules DJControl Mix Ultra MIDI mapping for Mixxx. Worker in progress using djercula reference. A6 validated (USB-MIDI confirmed on Pi).
- **TK-062 revived (2026-03-10).** Owner requested -24dB speaker gain as priority #2. Previously deferred during recovery.
- **Owner priorities after DJ test PASS (2026-03-10):** (1) Hercules controller mapping (TK-065, in-progress), (2) -24dB speaker gain (TK-062, open), (3) Web monitoring dashboard (TK-063, deferred/preliminary), (4) Automated visual regression testing (future story idea -- not yet filed as task, needs PO story refinement).
- **Future story idea: Automated visual regression testing.** Owner idea for screenshot-based regression testing of Mixxx/Reaper GUI rendering after system changes. Not yet scoped -- track as potential future story during backlog grooming.
- **TK-021 (T3d) pending closure (2026-03-10).** Owner considers T3d satisfied by 30+ min continuous DJ playback with zero xruns. AE and QE reviewing for formal sign-off. If approved, US-003 T3d moves to done. US-003 status will be updated after sign-off.
- **TK-066 DONE (2026-03-10, 3/4).** Version upgrades complete: 148 system packages, PipeWire 1.4.2->1.4.9, Reaper 7.31->7.64. Post-reboot verification ALL PASS. **Mixxx 2.5.0->2.5.4 BLOCKED** -- requires Qt 6.9 (Trixie ships 6.8.2, pulling Qt from sid too risky). Mixxx stays at 2.5.0. CLAUDE.md/README.md version numbers updated.
- **SAFETY RULE (2026-03-10, owner directive): WARN BEFORE REBOOTS.** Resetting the USBStreamer produces full-scale transients through the 4x450W amplifier chain -- speaker damage and hearing risk. Owner MUST be warned and MUST approve before: (1) rebooting the Pi, (2) `systemctl restart camilladsp`, (3) any action that causes USBStreamer to lose its audio stream, (4) PipeWire restarts. Owner decides when safe (e.g., amps off, volume lowered). Recorded in CLAUDE.md. **NO EXCEPTIONS.** This affects TK-066 (version upgrades require reboots), TK-062 (CamillaDSP config change), and any future deploy cycles per D-025.
- **Mode unification test FAIL (2026-03-10, S-009 + S-010).** Tested quantum 256 / chunksize 256 for DJ mode -- not viable on Pi 4B. Mixxx CPU: 85% -> 175% (callback rate too fast for Mixxx + hardware GL). CamillaDSP CPU: 5% -> 34% (within budget, not bottleneck). Xruns: ~24/min (worse than 8:1 mismatch test). **Conclusion:** Two-config approach confirmed correct. DJ mode stays at quantum 1024 / chunksize 2048. Live mode stays at quantum 256 / chunksize 256. D-028 (unified config) not viable on current hardware. **Runtime state gap remains:** DJ quantum 1024 is set at runtime (`pw-metadata`), not persistent across reboot -- this cannot be eliminated via unification. Reinforces D-002 (dual chunksize) and D-011 (live mode parameters).
- **TK-067 filed (2026-03-10).** Reaper-always-on feasibility test. Owner approved. Architect 7-phase plan: (1) Reaper baseline CPU, (2) quantum 1024 / chunksize 256 gating test, (2.5) PipeWire graph stability, (3) dual-app coexistence, (4) JACK port routing, (5) connect/disconnect resilience, (6) APCmini as Reaper mixer, (7) unified CamillaDSP config. Phase 2 is the gate -- if chunksize 256 + quantum 1024 produces xruns, plan stops. Eliminates mode-switching if all phases pass.
- **TK-067 suspended for day (2026-03-10).** Phase 1a partial pass: Reaper idle CPU 4.4% (hardware GL). **F-023 FOUND: dual V3D GL apps cause GPU contention stall on Pi 4B.** Mixxx + Reaper both using hardware GL causes system stall (mouse barely moves, audio stalls). Killing Reaper -> immediate recovery. Reaper itself NOT the cause -- stalling predated Reaper launch and persisted after kill. **Mitigation:** `LIBGL_ALWAYS_SOFTWARE=1` for Reaper (llvmpipe), architect recommendation, pending test next session. CamillaDSP stall/resume in logs is a **HYPOTHESIS (not confirmed)** -- may be normal idle behavior when no audio flows, but needs verification: (a) does stalling stop when audio is actually flowing? (b) could it mask a real USBStreamer issue? (c) was stalling present before this session? **Unexplained audio path issue:** Mixxx WAS actively playing (waveform scrolling, transport active) but audio was NOT reaching headphones despite PipeWire connections appearing correct. `pw-top` showed QUANT=0 for Mixxx despite active playback. Reboot was needed to restore audio. **Possibly related:** Reaper launch/kill cycles during TK-067 testing may have disrupted PipeWire's internal state in a way that `pw-link` doesn't reveal. CM initially diagnosed "Mixxx not playing" but owner confirmed playback was active -- the problem was in the PipeWire audio path, not Mixxx. Two unresolved issues from this session: (1) CamillaDSP stall hypothesis, (2) PipeWire QUANT=0 / silent audio path. Both need investigation next session. **Next session:** Reaper with llvmpipe, re-run P1a, proceed to Phase 2. F-023 needs formal filing in defects.md (architect/PM action).
- **F-023 (new defect, 2026-03-10): Dual V3D GL GPU contention on Pi 4B.** Running two hardware GL applications simultaneously (Mixxx + Reaper) causes system-wide stall. Root cause: V3D GPU contention (Pi 4B has single V3D GPU shared between all GL clients). Related to but distinct from F-012 (which was RT-specific ABBA deadlock, now fixed). F-023 occurs on PREEMPT_RT with the upstream fix applied -- it is a resource contention issue, not a deadlock. Mitigation: run Reaper with software rendering (`LIBGL_ALWAYS_SOFTWARE=1`). Severity: Medium (only affects dual-app scenario, workaround available). Blocks: TK-067 Phase 1a completion.
- **Overnight batch (2026-03-11).** Four workers spawned (no Pi access, local dev with mock data, sandbox auto-allow). **Completed:** TK-068 (flake.nix, re-spawned on main after worktree cleanup), TK-069 (MIDI daemon + 60 tests + README + bug fix), TK-070 (web UI 13 files), TK-071 (room correction pipeline -- largest deliverable). All deliverables committed: `1ef0b9f` (TK-068), `64cf6f1` (TK-069), `e6c95db` (TK-070), `8742062` (TK-071), `5ed5198` (doc updates). Pending reviews: architect (all), QE (test suites), AE (DSP pipeline correctness). **DJ overnight stability record (S-018, 2026-03-11):** Kernel `6.12.62+rpt-rpi-v8-rt` (PREEMPT_RT), PipeWire 1.4.9, CamillaDSP 3.0.1, quantum 1024, chunksize 2048, config `dj-pa.yml` (symlinked 2026-03-10 14:36). Repo commit at last deploy: `8dc707a`. Duration: ~7h 49m. Mixxx PID 1533, running 7h 45m, 48.4% CPU, 18.5% mem. Result: 0 errors, 0 xruns, peak 64.2C. **PASS.** Note: no git repo on Pi — configs deployed via `deploy.sh`, repo lives on dev machine.
- **Morning briefing audit (2026-03-11).** Owner requested 4-question briefing. Results: (1) TK-062 speaker trim confirmed correct (AE cross-checked — -24dB on ch 0-3 only, monitors 4-7 untrimmed). (2) DJ test git revision traceability gap — CM capturing from Pi. (3) **Backlog filing gaps fixed:** F-023 formally filed in defects.md (dual V3D GL GPU contention), F-024 filed in defects.md (QUANT=0 / silent audio path after Reaper launch-kill cycles), TK-072 filed (pipewire-force-quantum.service boot reliability). (4) Overnight achievement summary delivered by user story and validation status. TK-064 Note updated: "Needs architect to decide formal filing (F-023 defect or design note)" — note that the F-023 assigned to TK-064's Loopback issue is a different F-023 than the GPU contention defect; TK-064's Loopback finding was fixed in `f6e941b` and may warrant a different identifier to avoid confusion.
- **US-037 Playwright scaffolding validation (2026-03-11).** Tests committed (`6e8d097`) but never run. First run exposed 4 bug categories: (1) `to_have_class()` regex needs `re.compile()` in Playwright Python (6 failures), (2) `to_have_screenshot()` doesn't exist in Playwright Python 1.57.0 — replaced with manual PNG comparison (4 failures), (3) Makefile `cd scripts/web-ui` paths incorrect (Makefile already in that dir), (4) `--headed` and `browser` fixtures conflicted with pytest-playwright builtins. All fixed. flake.nix updated with test deps (`11f9384`), test fixes + reference screenshots (`f949919`). conftest.py rewritten for pytest-playwright compatibility. **Result: 34 passed, 2 skipped (expected).** QE review APPROVED (0 blocking). TK-073 through TK-076 now legitimately done. **QE follow-up items (non-blocking):** QE-F1: Makefile `install-test-deps` missing numpy. QE-F2: `test_monitor_e2e.py` bypasses page fixture console error capture (needs `pi_page` fixture when e2e suite grows). QE also noted WebSocket reconnection test as future task. All three tracked for next backlog grooming.
- **Afternoon work tracks (2026-03-11, owner approved).** PM + PO aligned on 4 parallel tracks, all pure software (no Pi access today). **Track 1: Quality hardening reviews.** Architect reviewing TK-069 (MIDI daemon), TK-070 (web UI), TK-071 (room correction pipeline) architecture. QE reviewing test suites for TK-069/TK-070. AE reviewing TK-071 DSP correctness. TK-071 review also maps delivered code against US-008 through US-011 AC to determine story closure. Status: COMPLETE (all reviews done). **Track 2: US-014 Documentation Structure.** TW delivered. Committed `6ef1e3e`. Status: DONE. **Track 3: US-011b Speaker Profile Schema + Config Generator.** Worker B DONE — 38 tests pass. Deliverables: 4 identities, 3 profiles, config generator, tests. Architect APPROVED (2 minor non-blocking: mixer name cosmetic divergence, ported profile placeholder identity). Committed `23b4ee0`. Status: DONE. **Track 4 (stretch): US-015 Theory and Design Doc.** TW delivered — expanded `docs/theory/design-rationale.md` with room correction theory + time alignment sections. Committed `ba15cfe`. Status: DONE.
- **Track 1 architect review results (2026-03-11).** All 3 deliverables architecturally sound — no rework needed. US-008 through US-011 AC mapping: US-008 ~40% (hardware interface stubbed), US-009 ~70% (edge cases + CamillaDSP integration), US-010 ~80% (ported sub protection missing), US-011 ~85% (bandpass deferred). **No stories closeable yet.** Gap tasks filed: TK-077 (missing default_profile.yml), TK-078 (US-008 gaps), TK-079 (US-009 gaps), TK-080 (US-010 gap — ported sub protection), TK-081 (US-011 gap — bandpass, deferred). US-010 and US-011 are closest to closure — TK-080 is pure software (no Pi), TK-081 is Phase 2 deferred.
- **Track 1 AE DSP review results (2026-03-11).** TK-071 conditionally approved — 4 mandatory fixes before deployment. Tasks filed: TK-082 (M-1 CRITICAL: multi-position + spatial averaging not implemented, US-008 AC error — "complex average" should be "magnitude average"), TK-083 (M-2: inverse sweep dead code with normalization bug), TK-084 (M-3: windowing transition_freq 300->500Hz), TK-085 (M-4: UMIK-1 calibration never called in pipeline). 6 informational notes (non-blocking). **Pure software tasks:** TK-083, TK-084, TK-085 are all doable without Pi. TK-082 partially (averaging algorithm yes, multi-position recording needs Pi). **US-008 AC corrected** by PO (commit `e304eea`): "complex average" -> "magnitude average" per AE.
- **Workers spawned for DSP fixes + US-011b (2026-03-11).** Three parallel workers, no file overlap. **Worker A (DSP quick fixes):** TK-077, TK-083, TK-084. Status: DONE (50 pass, 1 pre-existing failure TK-086). Committed `d36721c`. **Worker B (US-011b):** Speaker profile schema + CamillaDSP config generator. Deliverables: 4 speaker identity files (`configs/speakers/identities/`), 3 speaker profile files (`configs/speakers/profiles/`), `config_generator.py`, 38 tests (all passing), flake.nix updated with pyyaml. Status: DONE. Architect APPROVED (2 minor non-blocking). Committed `23b4ee0`. **Worker C (TK-080+TK-085):** Ported sub protection + UMIK-1 calibration integration. Status: DONE (111 passed cumulative, 1 pre-existing TK-086). Committed `0097f7a` (TK-080) + `9720659` (TK-085). **All three workers complete.** Documentation tracks committed: `6ef1e3e` (US-014) + `ba15cfe` (US-015). AE review pending for TK-080 and TK-085.
- **Architect US-011b review APPROVED (2026-03-11).** 2 minor non-blocking findings (mixer name cosmetic divergence, ported profile placeholder identity). No rework needed. Committed `23b4ee0`. **AE M-2/M-3 CONFIRMED RESOLVED** — AE verified both in codebase (`d36721c`). AE also confirmed TK-080 and TK-085 approved. **All reviews complete.** Only M-1 (TK-082, multi-position + spatial averaging) remains as mandatory fix — partially needs Pi. All pure-software afternoon work COMPLETE.
- **Web UI walkthrough readiness + next priorities (2026-03-11).** Owner asked: "Can I open a browser and walk through the web UI?" Answer: YES for Stage 1 scope (Monitor + System views functional with 5 mock scenarios). Measure and MIDI are intentional Stage 2 stubs. PM/PO/UX/QE aligned on priorities: (1) TK-087: QE-F1 numpy Makefile fix (trivial), (2) TK-088-091: Monitor UX hardening — 4 D-020 spec deviations found by UX review (signal-flow grouping, auto-hide dead capture meters, dB scale labels, clip latch), (3) TK-092: clickable Measure prototype (post-demo, owner-triggered). Deferred: MIDI view (daemon API not settled), controls/gain/mute (needs CamillaDSP write path), F-UX-002/F-UX-004/F-UX-007 (LOW). UX also found 7 findings total (F-UX-001 through F-UX-007); 3 are D-020 spec deviations, 1 professional standard, 2 LOW enhancements, 1 edge case needing Pi verification. Workers spawned for TK-087 + TK-088-091.
- **Track 1 QE test suite review results (2026-03-11).** All APPROVED. No HIGH or MEDIUM findings. **TK-069 MIDI daemon:** 60 tests, 3 LOW (find_hw_port untested, main() untested, action functions mocked-only). **TK-070 Web UI:** 132 tests (38 unit + 34 e2e + parameterized), 7 LOW (freeze_time unit test gap, misleading test name, install-test-deps numpy, e2e console error bypass — overlaps QE-F1/QE-F2 from US-037). **Track 1 COMPLETE.** All 3 deliverables approved by architect, AE, and QE. No rework required. QE LOW findings tracked for future cleanup pass alongside earlier QE-F1/QE-F2/WebSocket reconnection items.
